<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springproject.archive.mapper.BlogMapper">

    <insert id="insertBlog" parameterType="com.springproject.archive.dto.Blog">
        INSERT INTO blog (user_id, title, content, thumbnail_url)
        VALUES (#{userId}, #{title}, #{content}, #{thumbnailUrl})
    </insert>

    <select id="selectBlogList" resultType="com.springproject.archive.dto.Blog">
        SELECT id, user_id, title, content, thumbnail_url, created_at
        FROM blog
        ORDER BY created_at DESC
   	</select>
</mapper>
